document.addEventListener("DOMContentLoaded", function() {
    const element = document.querySelector('.seu-elemento'); // Ajuste o seletor conforme necessário
    if (element) {
        element.addEventListener('click', function() {
            // Seu código aqui
        });
    }
});
